<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Single branded splash, held on screen (via MainActivity's
         setKeepOnScreenCondition) until Flutter signals real content is
         ready over the "app/splash" channel — no native-then-Flutter
         double splash, no black gap during engine boot. -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
        <!-- API 33+: show the splash icon for notification/deep-link launches
             too. Default behavior skips the icon for those starts, which
             looks like a blank yellow screen before the first Flutter frame. -->
        <item name="android:windowSplashScreenBehavior">icon_preferred</item>
        <!-- Starting window / notification cold start. Theme.SplashScreen
             attributes are not applied to the first frame of a
             notification-started activity, so this is the actual first
             paint — yellow + centered logo, matching the splash. -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Platform API 31+ copies of the AndroidX splash attrs. Needed
             so the system splash (not just the compat library) uses yellow
             instead of the default light/white window. -->
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
    </style>
    <!-- Applied once the splash is dismissed; keeps the same background so
         there is no flash while Flutter's first real frame composites in. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <!-- Draw behind the system nav so Flutter can report a real bottom
             inset (gesture pill vs 3-button). Status bar is already handled
             by the Flutter overlay; only the nav bar needs this. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Translucent, Assistant-style overlay for QuickRecordActivity. Dims
         whatever is behind it (home screen / other app) while the mic
         sheet floats above, matching the "Hey Google" mic overlay feel. -->
    <style name="Theme.Duo.QuickRecord" parent="android:Theme.Black.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
